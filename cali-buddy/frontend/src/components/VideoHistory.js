import { Box, Heading, Text } from "@chakra-ui/react";

const VideoHistory = () => {
  return (
    <Box>
      <Heading size="lg">Video History</Heading>
      <Text>This is where users can view their past uploaded videos.</Text>
    </Box>
  );
};

export default VideoHistory;
